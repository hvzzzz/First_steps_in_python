import numpy as np
from numpy import random as rn

for i in range(5):
    #print(i)
    arr = np.arange(i)  # [0, 1, 2, 3, 4]
    #random.seed(1)  # Reset random state
    #rn.shuffle(arr)  # Shuffle!
    #print (arr)